import {StyleSheet} from "react-native";
import {colors} from "../../config/styles";

export default StyleSheet.create({
    textinput: {
      height: 30,
      width: 200,
      borderWidth: 0.5,
      padding: 10,
      borderRadius: 2,
      margin: 2
    }
})